Promise.resolve().then(() => _interop_require_wildcard(require(`1`)));

Promise.resolve(`${tag`2`}`).then(s => _interop_require_wildcard(require(s)));

Promise.resolve(`3-${MODULE}`).then(s => _interop_require_wildcard(require(s)));
